import classes from '@/styles/Home.module.scss';

const LandingPage = () => {

    return <section className={classes['landing-page']}>
        <p>Foobar</p>
    </section>
}

export default LandingPage;